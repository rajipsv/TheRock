# Copy to .env and fill in (never commit .env)
# cp .env.example .env   # Linux/macOS
# copy .env.example .env # Windows

# GitHub PAT — required for --pr, --full-manifest, and upstream_pr_scan.py
# Scopes: public_repo (minimum); add repo if analyzing private forks
# Fine-grained: Contents (read), Pull requests (read), Metadata (read)
GITHUB_TOKEN=

# Optional LLM executive summary (notebook section 9 / summarize.py)
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
# SUMMARY_LLM_BACKEND=openai

# vLLM on MI300 (AMD AI Agents 101 workshop pattern — see hackathon_demo.ipynb section 9)
# VLLM_BASE_URL=http://localhost:8000/v1
# VLLM_MODEL=Qwen3-30B-A3B
# OPENAI_API_KEY=abc-123
