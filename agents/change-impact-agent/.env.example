# Copy to .env and fill in (never commit .env)
# cp .env.example .env   # Linux/macOS
# copy .env.example .env # Windows

# GitHub API — read public ROCm repos (TheRock, rocm-libraries, PR fetch)
GITHUB_TOKEN=

# Optional LLM backends for summarize.py
# OPENAI_API_KEY=
# NVIDIA_API_KEY=
